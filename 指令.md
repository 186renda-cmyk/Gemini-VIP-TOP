我们现在处于一个具体的网站项目文件夹内。请帮我编写一个工业级的关键词挖掘脚本 miner.py。

核心目标： 读取当前目录下的 seeds.txt，利用双搜索引擎接口，深度挖掘长尾词，构建企业级词库。

功能需求详情：

输入源： 读取当前目录下的 seeds.txt（支持多行）。

双引擎并发架构：

Google 接口： http://google.com/complete/search?client=chrome&q={keyword}&hl=zh-CN

Bing 接口： https://api.bing.com/osjson.aspx?query={keyword}

脚本必须同时请求这两个接口，以获取最大的覆盖面。

递归挖掘策略 (Deep Mining)：

Level 1 (种子)： 直接搜种子词。

Level 2 (后缀遍历)： 在种子词后自动追加 a-z 和 0-9（例如 "Gemini a", "Gemini 1"...），抓取联想词。

Level 3 (递归扩展)： 请在代码开头设置一个开关 DEEP_MINING = True。如果开启，将 Level 2 挖到的前 5 个最长关键词作为新的种子，进行第三轮挖掘。

数据清洗与存储：

去重： 必须去除重复关键词。

实时保存： 结果写入当前目录的 raw_keywords.csv。

CSV 格式： Keyword (关键词), Source_Engine (来源: google/bing/both), Original_Seed (原始种子)。

工程化体验：

使用 fake_useragent 随机切换 UA 防封。

使用 tqdm 库在终端显示清晰的挖掘进度条。

请求之间加入 0.5-1.5秒 的随机延迟。

网络请求要是失败，捕获异常并跳过，不要让脚本崩溃。

请给出完整的 Python 代码和 requirements.txt。

第二步：构建素材整理工作台 (cleaner.py)

接下来编写 cleaner.py。这个脚本不仅要清洗数据，还要为我生成一份**“内容素材搜集工作表”**。

功能需求：

黑名单过滤 (Blacklist)：

内置硬编码黑名单：['星座', '运势', '性格', '配对', '男', '女', '生日', 'NASA', '天文', '股票', '基金']。

读取当前目录下的 raw_keywords.csv，如果包含这些词，直接删除。

智能意图分类 (Intent Classification)：

Transactional (高价值): 包含 ['买', '号', '价格', '充值', '合租', '拼车', '会员', '升级', '便宜']。

Informational (教程/引流): 包含 ['怎么', '教程', '注册', '下载', '登录', '入口', '失败', '验证', '节点', '梯子']。

General (其他): 不符合以上两类。

输出文件 1：机器数据 (final_tasks.csv)

包含 Keyword, Intent, Status (默认为 Pending)。

输出文件 2：人工工作台 (research_plan.md) —— 重点！

请生成一个 Markdown 文件，格式必须方便我复制关键词去小红书/知乎搜索。

排版格式如下：
```markdown
# 🦁 Gemini 网站内容素材搜集表
> 自动生成时间：{当前日期}
```

## 💰 必争的变现词 (Transactional)
*提示：重点搜索价格对比、防骗指南、购买渠道测评*
- [ ] **关键词：{keyword}**
  - [ ] *素材来源 1 (链接/笔记):*
  - [ ] *素材来源 2 (链接/笔记):*

## 📈 必写的引流词 (Informational)
*提示：重点搜索最新教程、报错解决方法*
- [ ] **关键词：{keyword}**
  - [ ] *素材来源 1:*
  - [ ] *素材来源 2:*
```

请给出完整代码。