Trae，我在审计 sitemap.html 时发现了一个严重的语法错误。

问题描述： 文件末尾出现了代码重复。在第 318 行的 </html> 闭合标签之后，又重复出现了一段 Footer 代码和 <script> 标签。

推测原因： 这很可能是 build.py 在生成或写入 sitemap.html 时逻辑有误。可能是使用了“追加模式 (a)”而不是“覆盖模式 (w)”写入文件，或者是模板拼接逻辑重复添加了 Footer。

请执行以下修复：

1. 修正 build.py

检查 update_sitemap.py 或 build.py 中负责生成 sitemap 的函数。

确保文件写入时使用的是 覆盖模式 (w mode)，例如 open(file, 'w', encoding='utf-8')。

确保在拼接 HTML 字符串时，没有重复追加 footer 变量。

2. 重新生成

运行脚本，重新生成 sitemap.html。

验证： 确保新生成的 sitemap.html 以 </html> 结尾，后面没有任何多余的代码。