请帮我修改 index.html，移除所有的平滑滚动库 (Lenis)，回归浏览器最原生的滚动体验，但保留页面元素的“渐入动效”。

请执行以下 2 步操作：

1. **修改 CSS (回归原生)**：
   请在 `<style>` 中，删除所有关于 `.lenis` 的样式。
   同时，为了让点击导航栏跳转时依然平滑，请确保 `html` 标签有以下样式：
   ```css
   html {
     scroll-behavior: smooth; /* 仅在点击锚点跳转时平滑，平时滚动由系统接管 */
   }
   /* 保留原本的 reveal 动效样式 */
   .reveal {
     opacity: 0;
     transform: translateY(30px);
     transition: all 0.8s ease-out;
   }
   .reveal.active {
     opacity: 1;
     transform: translateY(0);
   }

   简化 JS (只留动效)： 请删除底部引入 lenis.min.js 的那行代码，以及所有 new Lenis(...) 相关的初始化代码。 只保留 IntersectionObserver (滚动监听) 的逻辑，用于触发 .reveal 动画：
   <script>
  // 只保留视觉动效，滚动交给浏览器原生
  document.addEventListener('DOMContentLoaded', () => {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('active');
          observer.unobserve(entry.target); // 动画只播一次
        }
      });
    }, { threshold: 0.1 });

    // 选取所有需要动效的元素
    document.querySelectorAll('section, h2, .glass-card, footer').forEach(el => {
      el.classList.add('reveal');
      observer.observe(el);
    });
  });
</script>